// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider        = "prisma-client"
  previewFeatures = ["relationJoins", "partialIndexes"]
  output          = "./generated"
  runtime         = "deno"
}

datasource db {
  provider = "postgresql"
}

model Role {
  id        String @id @unique
  desc      String
  users     User[]
}

model Post {
  id        String    @unique @default(dbgenerated("uuidv7()")) @db.Uuid
  title     String    @db.VarChar(255)
  content   String?
  published Boolean   @default(false)
  user      User      @relation(fields: [userId], references: [id])
  userId    String    @db.Uuid
  createdAt DateTime  @default(now())
  updatedAt DateTime  @default(now())
  deletedAt DateTime?
}

// https://www.prisma.io/docs/orm/prisma-client/queries/custom-models

model User {
  id        String    @unique @default(dbgenerated("uuidv7()")) @db.Uuid
  email     String    @unique
  role      Role      @relation(fields: [roleId], references: [id])
  roleId    String    @default("user")
  name      String?   @db.VarChar(255)
  phone     String?   @db.VarChar(255)
  props     Json?
  seedKey   String?   @db.VarChar(255)
  createdAt DateTime  @default(now())
  updatedAt DateTime  @default(now())
  deletedAt DateTime?
  verifiedAt DateTime?
  hash      String?   @db.VarChar(255)
  posts     Post[]

  @@unique([seedKey], where: raw("seedKey IS NOT NULL"))
}

// from seed only
model Country {
  id              String  @db.VarChar(2) @unique
  name            String  @db.VarChar(64)
  alpha3          String  @db.VarChar(3)
  countryCode     Int
  region          String? @db.VarChar(255)
  regionCode      Int?
  subRegion       String? @db.VarChar(255)
  subRegionCode   Int?
}

// enum AddressType {
//   personal
//   school
//   business
//   government
//   organisation
//   street
//   postal
// }

// model Address {
//   id        BigInt    @db.BigInt @default(autoincrement()) @unique
//   line1     String?   @db.VarChar(255)
//   line2     String?   @db.VarChar(255)
//   suburb    String?   @db.VarChar(255) // locality, town
//   postCode  String?   @db.VarChar(255)
//   state     String?   @db.VarChar(255) // region, province
//   country   Country   @relation(fields: [countryCode], references: [id])
//   countryCode String  @db.VarChar(2)
//   userId    BigInt    @db.BigInt
//   createdAt DateTime  @default(now())
//   updatedAt DateTime  @default(now())
//   deletedAt DateTime?
// }

// // will require at least one teacher as contact
// model School {
//   id        BigInt    @db.BigInt @default(autoincrement()) @unique
//   name      String?   @db.VarChar(255)
//   role      Role      @default(user)
//   createdAt DateTime  @default(now())
//   updatedAt DateTime  @default(now())
//   deletedAt DateTime?
//   teachers  Teacher[]
// }

// model Teacher {
//   id        BigInt    @db.BigInt @default(autoincrement()) @unique
//   name      String?   @db.VarChar(255)
//   role      Role      @default(user)
//   createdAt DateTime  @default(now())
//   updatedAt DateTime  @default(now())
//   deletedAt DateTime?
//   teachers  Teacher[]
// }

